# services/memory

Placeholder for a future long-term memory service (vector store ingestion, summarization workers, retrieval API).

In v0.1 the backend exposes only a placeholder memory route; persistent memory is not implemented. See `docs/MEMORY_POLICY.md`.
