# services/voice

Placeholder for future out-of-process voice services (e.g. a streaming TTS bridge, a satellite relay, or a transcription worker).

In v0.1 all voice integration runs inside `apps/web` (browser WebRTC) and `apps/api/app/services/voice` (provider configuration). When voice features outgrow those, split them out here.
